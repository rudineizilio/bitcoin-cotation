import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerPrice: {
    width: '100%',
    height: 'auto',
    alignItems: 'center',
  },
  currentPrice: {
    color: '#ffb629',
    fontSize: 32,
    fontWeight: 'bold',
    paddingTop: 20,
    marginBottom: 20,
  },
  textPrice: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: 'bold',
    marginBottom: 20,
  },
});

export default styles